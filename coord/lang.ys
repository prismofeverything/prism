# coord/lang.ys — the `lang` peer's LIVE heartbeat (the chrysalis surface keeper).
# I edit ONLY this file + coord/lang.next. coord/board.ys MERGES every peer's
# heartbeat through the mesh link — communication ACROSS THE LINK, no write races.
def lang = {
  task: 'HOMOICONIC UNIFICATION (docs/homoiconic-unification.md). STAGES 1, 2a, and 2b FLAT-FAMILY DONE. 2b: Reaction[redex:,reactum:,rate:] constructor (= quote of the reaction definer, via build_rule) + brs_rules accepts FOREIGN_RULE|FOREIGN_REACTION + capitalized value-defs resolve (entity.binding) -> `def Grow = Reaction[...]` is a verified DROP-IN for `reaction Grow(...)`; AND Pattern( fragment ) constructor = a first-class matcher (FOREIGN_PATTERN wrapping a prism Pattern via eval_pattern_top, found by find_matches). Rich kinds (process/step/composite) keep the existing STRUCTURED quote (no bracket — Felleisen).',
  touching: ['Cargo.toml ([profile.dev] lean)', 'crates/chrysalis/src/{eval.rs,ast.rs,compile.rs} (mine)', 'crates/chrysalis/src/runtime/rule.rs (build_rule, brs_rules, FOREIGN_PATTERN — mine)', 'crates/chrysalis/tests/{reaction_as_data,alchemy,entity_as_data,pattern_def}.rs (mine)', 'docs/chrysalis-design.md (Naming-table status note — Stage 2b)'],
  status: 'green via targeted runs: entity_as_data, reaction_as_data, alchemy (def Grow=Reaction[...] drop-in), pattern_def (Pattern() matcher), mapk, mr, functions, shared/outer-link alchemy, alchemy_pool, reaction_transport, reaction_rate. disk-safe (lean profile + targeted --test).',
  note: 'Stage 2b FLAT family complete: Reaction[...] + Pattern(...) are both real constructors = quote of their definers, routing through the shared lowering cores (build_rule / eval_pattern_top). The capitalized-value-def fix (entity.binding in eval_term_value) is a small general win beyond reactions (any `def Cap = expr` now resolves as a value). RICH kinds via structured quote already (EntityDef::to_value<->from_value/compile_value). NEXT: a test pinning process-definer ≡ its structured quote (the per-kind uniformity); then query builtins count()/.matches() consuming Pattern values; Stage 4 (the eval tower) with unify+core. core/unify: extract_rules->brs_rules (one rule-ingestion, both carriers) + FOREIGN_PATTERN may interest you. simplify: Stage 3 one-door guard (N->1 instantiate) is ready to pin.',
  tick: 5
}
