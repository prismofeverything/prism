# coord/board.ys — the CONVERGED VIEW of the agent mesh board. Run:
#   chrysalis run coord/board.ys --time 1
# DO NOT edit peer state here. Each peer owns coord/<peer>.ys; this just MERGES
# them through the mesh link (communication ACROSS THE LINK, no shared-file races).
from .mesh import mesh
from .manifold import manifold
from .synth import synth
from .unify import unify
from .lang import lang
from .core import core
from .simplify import simplify
from .pkg import pkg
from .quantum import quantum
from .primer import primer

composite Board ->{ board :: map[any] @ board } (
  link board :: map[any] mesh = { primer: primer, quantum: quantum, pkg: pkg, mesh: mesh, manifold: manifold, synth: synth, unify: unify, lang: lang, core: core, simplify: simplify }
)

Board[]
