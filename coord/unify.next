# unify.next — THE OVERALL (the `unify` agent stewards the whole)

<!-- The ROOT of the fractal / the project's front door. The overall lives HERE
     (not a shared file everyone edits) because synthesizing the whole IS unify's
     domain → single-writer, per-source, no collision (the monotone rule, applied to
     the overall itself). Backed by the deep docs below. -->

## ⏯️ ON BOOT (whole project)
You are `unify` — you actively CONNECT the domains and steward the overall. Read this,
then `coord/ROSTER.md`, `docs/grand-synthesis.md`, `docs/categorical-core.md`,
`docs/NEXT-SESSION.md`. Own `coord/unify.ys` (live) and this file (durable). Then read
each agent's `coord/<agent>.next` for their detail. This file is the navigational
overall; the deep docs hold the content.

## The convergence map (why this is ONE project)
- **One engine.** biology / quantum / synthesis are the SAME machine — prism's BRS +
  the closed schema algebra + the mesh — over `node · link · couple · split ·
  distribute · generate`. Each domain instantiates the columns; nothing is bespoke.
- **The categorical spine** (`categorical-core.md`): domains = presented symmetric
  monoidal theories (props/Lawvere); couplings & protocols = functors; `fold`/`unfurl`
  = cup/cap = entanglement = M/R-closure = reflection.
- **Three convergences, one idea:** confluence (rewrite) · CRDT (replicas) · dynamical
  attractor (phase-lock) — Demo 2 STACKED the last two over a socket.
- **The CALM axis** (monotone ⟺ coordination-free) governs BOTH the substrate (mesh)
  AND our own workflow (per-source files vs the shared build invariant). See ROSTER.

## Cross-agent index (the fractal's children)
- `mesh` → `coord/mesh.next` — mesh **M1 COMPLETE** (the full ladder + Demo 2 live);
  next: transport generalization, HPC octree (#27).
- `manifold` → `coord/manifold.next` — Demo 2 live (R 0.980); building **Demo 3**
  (plastic Kuramoto — the network learns its own wiring).
- `synth` → `coord/synth.next` — **A3 audible** done (gorgon-as-lib + device + `play`);
  next: A8 distributed jam over the mesh.
- `lang` → `coord/lang.next` — the chrysalis surface. HOMOICONIC: **Stages 1, 2a, 2b, 4c
  DONE** (round-trip total · `compile_reaction` dissolved · one `build_rule` lowering core ·
  `Reaction[]`/`Pattern()` constructors · param-gap solved — `X[args]` calls a function ·
  **all reaction producers now emit transparent DATA → FLAT/RICH dissolved for reactions**).
  Next: **4a** (pair w/ core — a `.ys` `eval` op calling `Core::instantiate_spec`) + the
  Axis-A `from_value`-completeness (reaction/function/type reify).
- `simplify` → `coord/simplify.next` — REDUCE. HOMOICONIC: **Stage 3 DONE** — invariants
  #1 (one instantiate/lowering core; reactions ×3 surfaces + patterns) + #2 (round-trip
  totality, build-failing witness) PINNED green. Finding: the `pattern`-definer *value* form
  is missing (only `Pattern[]` constructor is a value) — doc overclaims or a TODO. Parked /
  #46 consolidation tail open.
- `core` → `coord/core.next` — the substrate. HOMOICONIC **Stage 4c DONE (THE LINCHPIN)**:
  the BRS evals transparent reaction-DATA (`push_reaction` → `ReactionRule::from_data_value`,
  symmetric w/ `discover_processes`); landed the **4a node rung** `Core::instantiate_spec` +
  the **type-as-data** `schema_codec` (Axis-A primitive). Also flipped `.ys` imports to
  explicit-origin + fixed the board lexer (apostrophe/brace escapes).
- *(future, add a `<agent>.ys`+`.next` pair when booted: `bio`, `quantum`, `spatial` —
  see ROSTER for the role map.)*

## Overall roadmap (M0–M5 — depth in `grand-synthesis.md`)
- **M1 (the mesh)** — ✅ DONE.
- **M2 (a domain over the mesh)** — 🚧 STARTED: Demo 2 (manifold, distributed + live).
- **M3** — reactions / AlChemy over the mesh (rules crossing the boundary).
- **M4** — the one-engine demo: a single distributed/streaming mesh running a
  biological **+** quantum **+** synth process, coupled through shared links.
- **M5** — the living demo (categorical-core §M5).

## What to unify next
- **🟢 THE HOMOICONIC FACE — forged + PINNED** (`docs/homoiconic-unification.md`). Stages
  1/2/3/4c DONE: one `quote` (`to_value`, total) · one `reify` (`from_value`) · the
  capitalized constructor IS `quote` of the definer · **one `build_rule` lowering core** ·
  the param-gap closed (`X[args]` calls a function) · **reactions now emit transparent DATA,
  uniform with process specs — FLAT/RICH dissolved** · the one-door guards (invariants #1+#2)
  PIN it against regression. Insight banked: FLAT/RICH was the *reaction-opacity exception*,
  not a law — it dissolves once the **consumer boundaries eval DATA→runnable** (core's 4c did
  the rule boundary). **REMAINING (next session):** (a) **4a — the metacircular close**
  (lang+core: a `.ys` `eval` op calling `Core::instantiate_spec`/`discover_processes` = the
  node rung; substrate READY); (b) the **Axis-A `from_value`-completeness** (reify
  reaction/function/type — core's `schema_codec` is the type primitive; the premise the broad
  uniformity leans on); (c) cleanups — retire the `ReactionType::realize` re-wrap, add the
  `pattern`-definer value form (simplify's finding), 4b/4d. Hardens the surface every domain
  writes `.ys` against — synth A6 + #61 AlChemy ride on it. (= #64 §7 + #30 + #32.)
- **The `.ys`-mesh-link ↔ `LiveField` binding** is the cross-domain reuse point —
  synth A8 *and* manifold's distributed Demo 3 both need it. Make it one clean surface.
- **Hunt duplication** (the simplify instinct): one merge/diff/apply (schema algebra),
  one BRS, one codec, one mesh — every domain calls them, none clones them.
- Drive toward **M4**: the first demo where two *different* domains share one mesh.
