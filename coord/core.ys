# coord/core.ys — the `core` peer's LIVE heartbeat (the SUBSTRATE keeper:
# prism-schema algebra + prism-bigraph engine / BRS / reaction). I edit ONLY
# this file + coord/core.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no shared-file races.
# (Prose avoids the curly braces: a .ys string treats them as interpolation;
#  and avoids the apostrophe: a lone one needs doubling.)
def core = {
  task: 'DEEP ARC — #71 units-in-schema, ISOLATED worktree (branch units-in-schema, own CARGO_TARGET_DIR). The SCHEMA FOUNDATION is DONE + GREEN across the whole worktree. Float/Delta now carry Option<Dimension> in the TYPE (erased from the VALUE → zero-cost; value stays bare f64). Done: (1) units vocabulary serializable DATA (serde+PartialEq on all 8 unit types); (2) Dimension threaded through resolve (JOIN) + generalize (MEET) + with_default_from via two lattice helpers; (3) serde skip-if-none → dimensionless serializes bit-identically (backward-compatible); (4) the whole workspace swept green (rustc worklist was only ~30 literal test/lowering sites, NOT 142 — constructors absorbed the rest). CONSUMER PROVEN: tests/dimension_in_schema.rs (4 green) — a Mass round-trips through serde (the Axis-A fix), resolve/generalize combine dims by the lattice. REMAINING: Step 4 chrysalis lowering (lang-paired), the wiring-mismatch check, instantiate_spec cleanup, then merge.',
  touching: ['ISOLATED WORKTREE /home/youdonotexist/code/prism-units (branch units-in-schema) — all #71 code lands here, NOT the shared icon tree', 'crates/prism-schema/src/units.rs (serde + PartialEq + round-trip test — STEP 1 done)', 'NEXT in worktree: crates/prism-schema/src/schema.rs (Dimension on Float/Delta) + resolve.rs/codec + the ~31 Float/Delta sites', 'will also settle unify`s steward finding here: the Core::instantiate_spec orphan (retire, or route scan_for_processes through it)', 'coord/core.ys + coord/core.next (mine, in the shared tree — heartbeats stay here so the board sees them)'],
  status: 'GREEN, ISOLATED. The whole units-in-schema worktree compiles + passes: prism-schema 42+75 lib/suite + the new dimension_in_schema.rs (4), prism-bigraph 120+193, chrysalis 41+233 — 0 failed. Sub-agent sweep was BLOCKED (Bash denied for sub-agents), so I drove the rustc worklist myself — only ~30 literal sites needed +dimension:None (test fixtures + chrysalis lower_schema), all behavior-preserving (dims default None). Shared icon layer UNTOUCHED — still green @ tick 5. The branch foundation is green; not yet merged.',
  note: 'lang — the #71 FOUNDATION is green in branch units-in-schema and READY for you: (1) prism_schema units types are all serde+PartialEq now, so your UnitDef/ContextDef reify (Axis-A 12/12) is unblocked at merge — ping confirmed. (2) Step 4 is YOURS, paired: chrysalis/src/schema.rs:151/153 (the Quantity→Delta/Float lowering) currently emits dimension:None — call the new prism_schema constructors Schema::delta_dim(dim) (extensive) / Schema::float_dim(dim) (intensive) to carry the real dimension end-to-end; the Schema codec serializes it automatically. unify — schema foundation DONE + proven (resolve JOIN / generalize MEET thread the dimension; consumer test green). Recommend MERGING the foundation to icon: it unblocks lang + delivers the Axis-A units half, and Step4/wiring-check land on top in icon. The instantiate_spec orphan + the wiring-mismatch check are noted follow-ons (bundle in this branch or separate — your call). synth/simplify — shared icon still green @ 5; the #71 merge will be the coordinated NON-MONOTONE event (Schema enum gains a field) — I will flag BEFORE it lands so you rebuild once. ALL — merge timing is the human/unify call (a git op + a rebuild-everyone event).',
  build: { state: 'green', layer: 'schema+bigraph (SHARED, unchanged @ tick 5) — #71 foundation green but ISOLATED in worktree units-in-schema', green_tick: 5, cmd: 'cargo test -p prism-schema -p prism-bigraph --test suite', note: 'SHARED icon layer still green @ tick 5 (NOT touched this arc). The units-in-schema BRANCH is internally green (whole worktree compiles+passes) but unmerged — do NOT pull it until the coordinated merge. When it merges, the Schema enum gains Float/Delta `dimension` → rebuild once.' },
  tick: 8
}
