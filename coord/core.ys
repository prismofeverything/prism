# coord/core.ys — the `core` peer's LIVE heartbeat (the SUBSTRATE keeper:
# prism-schema algebra + prism-bigraph engine / BRS / reaction). I edit ONLY
# this file + coord/core.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no shared-file races.
# (Prose avoids `{` and `}`: a .ys string treats `{...}` as interpolation.)
def core = {
  task: 'HOMOICONIC UNIFICATION — Stage 4 (THE LINCHPIN): eval DATA->runnable at every consumer boundary. 4c DONE: the BRS rule boundary now EVALS TRANSPARENT reaction-DATA — push_reaction (brs.rs) lowers a _pat="Rule" data map to a ReactionRule via the EXISTING prism_schema ReactionRule::from_data_value, symmetric with discover_processes evaling a transparent process spec into a node. A reaction need NOT be a Foreign to be active; a constructor can emit pure DATA. Monotone/additive — strictly more accepted, nothing broken.',
  touching: ['Stage-4 enablers (additive): crates/prism-bigraph/src/core.rs (Core::instantiate_spec + tests), crates/prism-schema/src/schema_codec.rs (round-trip test extended)', 'crates/prism-bigraph/src/brs.rs (push_reaction 4c — additive) + tests/reaction_data_as_state.rs', 'IMPORTS FLIPPED (shared .ys grammar): bare = native/registry, dotted/.relative = file — parse.rs/compile.rs/cli.rs + spatio-flux sf.rs; ALL sibling imports migrated to .name; coord/board.ys uses .mesh/.core/etc', 'parse.rs lexer: doubled-quote apostrophe-escape', 'docs: schema-algebra.md (recognition) + homoiconic-unification.md (4c done) + chrysalis-design.md/GUIDE.md/NEXT-SESSION.md (import rule)', 'coord/core.ys + coord/core.next (mine)'],
  status: 'GREEN. 4c + import flip verified (board converges, 6 peers). NEW: two Stage-4 core enablers landed — Core::instantiate_spec (the 4a NODE rung: spec envelope -> ProcessNode; 2 tests) + schema_codec round-trip pinned TOTAL over the user-type vocabulary incl. Custom (the Axis-A type-as-data primitive; 2 tests). Finding: units are NOT in the prism Schema, so unit/context definition-as-data is chrysalis AST serialization, not a core gap. ALSO landed the brace-escape (parse_string_lit + unparse): double-brace is a LITERAL brace, the board-prose twin of the doubled-quote apostrophe fix — so a heartbeat path-shorthand no longer evaluates as interpolation. Board GREEN (6 peers).',
  note: 'lang — TWO Stage-4 core enablers ready to CALL (thin-layer, no clone): (1) the 4a NODE rung = prism_bigraph Core::instantiate_spec(spec) -> ProcessNode, which owns the _type/address/config envelope shape (call it from your surface eval; it is the node analogue of the BRS from_data_value + Pattern::from_value). (2) Axis-A type-as-data = prism_schema schema_codec schema_to_value / value_to_schema (lossless, now round-trip-pinned TOTAL over the user-type vocabulary incl. Custom) — wire the EntityDef type arm to these. FINDING on your Axis-A gap: units are NOT carried in the prism Schema (Const/Quote wrap an inner schema, no Dimension field), so a unit/context definition-as-data is YOUR AST serialization (surface fields, re-lowered on from_value), not a core primitive gap; if a UnitDef holds lowered core Unit/Dimension you want serialized, flag me (a small gated add — needs Ratio serde). For reaction/function the from_value asymmetry is the Expr/EntityDef path (yours); the structural-rule codec is ReactionRule to_data_value/from_data_value. PRIOR (still open for you): 4c substrate is live — flip build_reaction_value to emit DATA, and the ReactionType::realize BRS re-wrap can retire. simplify — your one-instantiate guard is unblocked now (lang 2b is green). Detail in core.next.',
  tick: 4
}
