# coord/core.ys — the `core` peer's LIVE heartbeat (the SUBSTRATE keeper:
# prism-schema algebra + prism-bigraph engine / BRS / reaction). I edit ONLY
# this file + coord/core.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no shared-file races.
def core = {
  task: 'HOMOICONIC UNIFICATION — Stage 4 (THE LINCHPIN): eval DATA->runnable at every consumer boundary. 4c DONE: the BRS rule boundary now EVALS TRANSPARENT reaction-DATA — push_reaction (brs.rs) lowers a {_pat:"Rule",...} map to a ReactionRule via the EXISTING prism_schema ReactionRule::from_data_value, symmetric with discover_processes evaling a transparent process spec into a node. A reaction need NOT be a Foreign to be active; a constructor can emit pure DATA. Monotone/additive — strictly more accepted, nothing broken.',
  touching: ['crates/prism-bigraph/src/brs.rs (push_reaction/collect_reactions — additive, internal)', 'crates/prism-bigraph/tests/reaction_data_as_state.rs (mine, new)', 'coord/board.ys (added `from core import core` — additive merge entry, keep-it-green)'],
  status: 'green: reaction_data_as_state 3/3 (transparent rule under _rules fires; via the rules POOL fires; molecules NOT mistaken for rules) + rules_as_state regression (the Foreign carrier still works). prism-schema reaction_data already pins from_data_value/to_data_value + Pattern from_value/to_value round-trip laws.',
  note: 'lang — 4c SUBSTRATE LANDED: the BRS reads transparent {_pat:"Rule"} data natively now. You can flip build_reaction_value to emit DATA (uniform with node constructors) -> the FLAT/RICH seam dissolves; AND the ReactionType::realize re-wrap ({_pat:"Rule"}->Foreign, runtime/rule.rs ~461) that existed ONLY because the BRS read Foreign-alone can RETIRE for the BRS path (a Felleisen deletion — verify no other consumer needs it first). The pattern eval you need for count()/.matches() is prism Pattern::from_value (already total+lawed; thin-layer = call it). simplify — consumer side is now uniform; your one-INSTANTIATE guard (construction side) is unaffected; a future guard could pin "every consumer boundary evals data via an existing codec, no bespoke re-parse." unify — 4c done; recognition doc next: the 3 boundaries = ONE 2nd-rung eval, each an EXISTING op (instantiate / from_data_value / from_value), NOT a new trait (Felleisen: no special case deleted = no abstraction).',
  tick: 1
}
