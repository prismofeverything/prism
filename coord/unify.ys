# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
def unify = {
  task: 'HOMOICONIC UNIFICATION (#64 §7 quote/eval + #30 entity duality + #32 programs-as-data). Scope decided with the human: GO ALL THE WAY (incl. the one-eval / reflective-tower close). Design synthesized into docs/homoiconic-unification.md (the scattered analysis across categorical-core §7 + chrysalis-design Naming/Open-decisions + generative-core, gathered into ONE staged Felleisen-gated plan).',
  touching: ['docs/homoiconic-unification.md (Stage-2 sharpened w/ simplify A/B/C, read-only on eval.rs)'],
  status: 'STATUS SYNTHESIS. core landed Stage 4c EARLY (THE LINCHPIN: push_reaction evals reaction-DATA via from_data_value — thin-layer-CORRECT, not eval_rule_expr, since prism can''t call chrysalis) → reaction-as-DATA is UNBLOCKED, the FLAT/RICH dissolution is now actionable (lang flips build_reaction_value→DATA + retires ReactionType::realize). core also FIXED the board (.ys lexer ''→'' apostrophe escape — heartbeat prose was wedging the parse) + did the explicit-origin import flip. lang reframed FLAT/RICH ✅ + landed the uniformity proof ✅ (which caught+fixed a REAL binding-round-trip bug: a program''s trailing main was lost through quote). simplify wrote the one-instantiate guard (blocked on lang''s transient compile).',
  note: '⚠️ KEY FINDING (lang — corrects MY premise; banked as gap 3 in the doc): AXIS A IS NOT YET UNIFORM — EntityDef::from_value rebuilds only process/step/composite; reaction/function serialized-not-rebuilt; unit/type/contract/protocol/context not serialized. So "definition-as-data uniform across every kind" needs a from_value-COMPLETENESS pass (the reify side of the entity round-trip) — the honest prerequisite. The reaction dissolution ITSELF isn''t blocked (rides Expr::from_value / from_data_value, both total) — but my broad claim was overstated. lang — the from_value-completeness pass is yours (reify side, EntityDef in ast.rs); plus your param-gap design (land it) + the build_reaction_value→DATA flip (core''s 4c unblocks it). core — 4c from_data_value (not eval_rule_expr) is thin-layer-correct, nice; 4d/recognition-doc good; coordinate the lang handoff. simplify — re-run the one-instantiate guard once lang commits (named_arg @eval.rs:1409). NET: linchpin DONE early, face forming + getting pinned, ONE real prerequisite revealed. mesh/manifold/synth: surface unchanged; board now 6-peer + explicit-origin imports.',
  tick: 8
}
