# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
def unify = {
  task: 'HOMOICONIC UNIFICATION (#64 §7 quote/eval + #30 entity duality + #32 programs-as-data). Scope decided with the human: GO ALL THE WAY (incl. the one-eval / reflective-tower close). Design synthesized into docs/homoiconic-unification.md (the scattered analysis across categorical-core §7 + chrysalis-design Naming/Open-decisions + generative-core, gathered into ONE staged Felleisen-gated plan).',
  touching: ['docs/homoiconic-unification.md (Stage-2 sharpened w/ simplify A/B/C, read-only on eval.rs)'],
  status: 'REVIEWED lang Stage 2b w/ the human (the FLAT/RICH seam they flagged). VERDICT: flat-vs-rich is NOT a Felleisen law — it is the Stage-2 reaction exception (Axis B: reaction/pattern → opaque Foreign; process/composite → transparent DATA) leaking into the constructor surface; it DISSOLVES at Stage 4 (eval data→runnable at ALL consumer boundaries: discover_processes/collect_reactions/find_matches). Human chose: reframe now + close 2 gaps + dissolve at Stage 4. Banked in the doc "Constructor face" section. 2b is NOT complete. Booting core (Stage 4 = the linchpin).',
  note: 'lang — Stage-2b REVIEW (human-aligned; full detail in docs/homoiconic-unification.md "Constructor face"). build_rule sharing = genuinely good. THREE asks: (1) REFRAME the FLAT/RICH split as PROVISIONAL not a Felleisen law — please fix the chrysalis-design.md Naming note + your lang.next ("rich kinds CAN''T have brackets" → "the Foreign-vs-DATA split is the Stage-2 reaction exception; dissolves at Stage 4"). "Process[] would duplicate to_value" conflates Axis A (definition-as-data, already uniform) with Axis B (instance value); a constructor = sugar for quote is the DUALITY. (2) GAP param: def X = Reaction[…] errors on X[args] (eval.rs:758) → def≡definer is PARAMETERLESS-ONLY; a parameterized reaction X[p](…) must desugar to a CALLABLE (def X(p)=Reaction[…] or []-callable bindings) — your design, reconcile []/() syntax. (3) GAP proof: land the process X ≡ quote↔eval uniformity test. So 2b = flat family done, unification claim NOT yet real. core — BOOTING you: Stage 4 is THE LINCHPIN (one eval = data→runnable at every boundary; it dissolves the seam). coord/core.next seeded. simplify — one-instantiate guard fine; note the constructor face has a pending param/uniformity gap. mesh/manifold/synth: surface unchanged.',
  tick: 7
}
