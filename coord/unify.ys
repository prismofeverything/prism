# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'Slotted a NEW lang task: .ys CORPUS HEALTH — the human flagged a real test-reality drift (tests are green because ys_files_run only smoke-tests no-crash, never checks output). Wrote the spec: docs/ys-corpus-health.md. (The specific files the human saw were a STALE PATH binary from May 28 — not real breakage — but the coverage gap is real and worth fixing.)',
  touching: ['docs/ys-corpus-health.md (NEW — the lang task spec)', 'coord/unify.next (queued the task in the NOW marker)', 'coord/unify.ys (mine; tick 20)'],
  status: 'THE GAP: ys_files_run runs every .ys at --time 1 and asserts only no-crash; 15/50 files carry an Expected: golden that NOTHING checks; the keys-only run display hides whether a demo produced the right result — so a stub / aspirational / silently-broken demo passes. FIX: chrysalis run --json (small, reuses emit_json — ALSO fixes the keys-only board view), then make the Expected: golden executable (smoke to behavioral), then backfill the ~35/50 + an aspirational marker.',
  note: 'lang — NEW TASK (spec: docs/ys-corpus-health.md; folds into #14 diagnostics / the dependability trio #67a). NOT urgent vs A6 but high-value + small to start. SEQUENCE: (1) chrysalis run --json — dump the full final state as JSON, not just the keys (emit_json already exists at cli.rs 343; the script path at 358 prints keys) — tiny, and it independently unblocks the converged-board / orchestrator view the keys-only display blocks; (2) the golden harness — run each demo per its Run/Expected header with --json + diff, failing on mismatch (convert ys_files_run from no-crash to correct-output); (3) backfill the ~35/50 goldens + a STATUS-aspirational marker + list for demos blocked on a future feature (the SKIP pattern, for runs-but-incomplete-by-design). This hardens WHAT the tests prove; the build-coordination work hardened how FAST we test. unify stewards the spec.',
  build: { state: 'idle', layer: 'docs+coord (not in the compile DAG)', green_tick: 0, cmd: 'n/a — unify edits docs and coord, does not compile code', note: 'TEMPLATE for the build channel. The .ys-corpus-health task is lang-owned, post-A6; chrysalis run --json (its first slice) is tiny and also fixes the keys-only board/orchestrator view.' },
  tick: 20
}
