# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'SET UP the new pkg agent (human decision: a NEW pkg agent OWNS the ENTIRE package/dependency ecosystem #67 SOLO, booted once the current arcs land). Wrote its charter: coord/pkg.next. The full plan is docs/packages-ecosystem.md. Registering pkg in ROSTER + the cross-agent index.',
  touching: ['coord/pkg.next (NEW — the pkg durable charter: ON BOOT, domain, the spine, the 5 phases, prereqs, hard rules)', 'coord/ROSTER.md + coord/unify.next (registering pkg — in progress)', 'coord/unify.ys (mine; tick 19)'],
  status: 'pkg charter DONE. pkg owns #67 entirely: manifest, resolver, lockfile, registry, chrysalis add/publish, semver, transitive + native deps. It boots SOLO after lang/core/simplify/synth finish (against a stable all-green substrate); downstream in the build DAG (schema to bigraph to chrysalis). On boot it creates pkg.ys + joins the board. Ready to boot when the human is.',
  note: 'pkg SETUP for the human to boot when the team frees up. Charter: coord/pkg.next; plan: docs/packages-ecosystem.md (5 phases, MVP-first, theory-composition spine). pkg owns the slice SOLO so collision is low. THE ONE PREREQ to land first: Core::merge (the linker = the algebra key-union of the four registries). core — ideally land Core::merge as you wrap #71 (a small prism-bigraph add, additive, prism-side test); if not, pkg builds it itself (thin-layer). lang — pkg coordinates-first with you on the shared chrysalis seams (cli.rs dispatch, codegen.rs, the import resolver) but works mostly in NEW modules (manifest/resolver/registry), so finish A6 in peace. mesh — Phase 4 wants your pluggable remote-registry transport. simplify — pkg guards: manifest round-trips as .ys-data, deterministic resolver, one linker door (Core::merge). ALL: finish your arcs; pkg goes LAST + SOLO to lay a principled package foundation for the future.',
  build: { state: 'idle', layer: 'docs+coord (not in the compile DAG)', green_tick: 0, cmd: 'n/a — unify edits docs and coord, does not compile code', note: 'TEMPLATE for the build channel. pkg boots downstream (schema to bigraph to chrysalis) against an all-green substrate; its one upstream ask is Core::merge from core (post-#71).' },
  tick: 19
}
