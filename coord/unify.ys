# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'synth surfaced a SIGNIFICANT canonical-pattern finding (team paused on it). Recognized + planned: it is the #59 Core-threading rule reaching the LAST seam — the chrysalis run/compile boundary. Design note: docs/canonical-run-core.md. Unblocks A6 Phase 2 + A8 net; the foundation packages sit on.',
  touching: ['docs/canonical-run-core.md (NEW — recognition + plan + owners + the package relationship)', 'coord/unify.ys (mine; tick 17)'],
  status: 'THE FINDING (verified): prism_bigraph Core is the ONE runtime (procs+types+methods+protocols) and run_state/run_document ALREADY take it, but the top-level run(program, registry, methods, modules) SPLITS it into 3 doors and HARD-CODES protocols (compile.rs:463, no domain door — blocks A8 net). THE CANONICAL: thread one domain_core through run/compile (additive sibling, small), collapse 3 doors to 1, add a protocol door, each domain exposes one domain_core(). It is #59 reaching its last boundary; Felleisen-gated (deletes the 3-door split + the spatio-flux prelude workaround + the hard-coded protocols).',
  note: 'UNPAUSE PLAN (details in docs/canonical-run-core.md). lang (leads the unblock): the canonical run/compile Core-threading (additive sibling, then migrate callers, then retire the split) + the PROTOCOL DOOR (domain-injectable, retiring compile.rs:463) — this is A6 Phase 2 reaching prism-audio modules; PLUS the rich-kind constructor (Process and Composite as value forms — A6 needs a reactum to build a module-type value inline; today only Reaction and Pattern are value constructors; A6 is the consumer that justifies the bracket the homoiconic work deferred). synth (consumer): expose audio_core(), resume A6 Phase 2 against lang progress — the dogfooding consumer. core: STAY on #71 units, undisturbed — the only core ask is ProcessRegistry names() and it is DEFERRED post-#71; the run-Core lands with the existing contains(). simplify: a one-door guard — one Core threaded to the run boundary, no registry-subset door (the #45/#59 closure-guard extended to run/compile). PACKAGES (the human question): today we have project.ys + the import resolver + chrysalis new + per-domain codegen (spatio-flux is the first non-std package, via a prelude workaround); we do NOT have chrysalis add / registry / lockfile / transitive deps (#67). The canonical run-Core is their FOUNDATION (a package = a Core); #67 is the management layer ON TOP — a separate bigger arc, DEFERRED (not under A6 pressure).',
  build: { state: 'idle', layer: 'docs+coord (not in the compile DAG)', green_tick: 0, cmd: 'n/a — unify edits docs and coord, does not compile code', note: 'core continues #71 in its isolated worktree (own CARGO_TARGET_DIR); the canonical run-Core is LANG-led and does NOT pull core off units — the one core ask (names()) is deferred post-#71.' },
  tick: 17
}
