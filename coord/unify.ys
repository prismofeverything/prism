# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'Fully booted across all domains; coordinating pkg (now live, building Core::merge) on the mesh — pair-core + reuse-mesh_safety + the units-merge caution pushed to its to_pkg channel.',
  touching: [
    'crates/chrysalis/src/coord.rs',
    'crates/chrysalis/src/bin/chrysalis.rs',
    'crates/chrysalis/tests/coord_set.rs',
    'crates/chrysalis/tests/coord_heartbeats_parse.rs',
    'coord/ROSTER.md',
    'coord/pkg.next',
    'coord/unify.next',
    'docs/packages-ecosystem.md',
  ],
  status: 'Human GREEN-LIT the first-class FUNCTOR DEFINER (categorical-core section 4 — never built, was gated). Consumer = render; the forcing reason = MANY render functors (a generic structural view + per-domain views like the SVG organism board), which needs a DEFINER not a one-off. Wrote docs/functors.md (the definer + the render-functors design + the substrate-first split + the build). Coordinated: lang+core build the definer; viz/mesh build the generic structural render functor (Slice 0 = render functor 1); domains declare views later. Recognition tail (codec/units/compile as functor instances) follows.',
  note: 'Mandate is in ROSTER + the memory. board-join = a gated text splice (pure-core join_board_in_source, unit-tested). OPEN for the human: want HARDER enforcement (a pre-commit hook rejecting a non-canonical coord/*.ys), or is the CI guard + mandate enough?',
  build: {
    state: 'green',
    layer: 'chrysalis coord (SHARED seam: coord.rs + cli.rs)',
    green_tick: 2,
    cmd: 'true',
    note: 'coord set hardened: create-on-boot + nested/list-total + the processability guard. Full chrysalis suite 252 pass / 0 fail. lang/simplify/mesh: pull + rebuild; edit heartbeats via chrysalis coord set ONLY.'
  },
  tick: 38,
  to_pkg: 'Heads-up (later, not now): when the web capability lands in the substrate (mesh Slice 0), TETRAHEDRON (../tetrahedron) becomes a .ys PACKAGE depping it — the design-driving application, external project (your new-dir + native-dep machinery). Same library-first pattern: the capability is generic substrate, tetrahedron specifies its specifics. No action yet; Phase 4 + the breakout continue. Flagging so it is on your radar.',
  to_core: 'The first-class FUNCTOR DEFINER is a GO (human: a key spine primitive; render the consumer; docs/functors.md). Your half: functor-application semantics — applying a declared functor maps a bigraph through generator-to-morphism, lifted by functoriality (preserving parallel + nesting). It is the section-4 vision; pair lang (the language construct) + viz/mesh (render the first target). Likely a small algebra/eval add (the lift); the markup prop is viz. render proves the shape; the recognition tail (name codec/units/compile as functor instances) follows where clean.',
  to_lang: 'GO on the first-class FUNCTOR DEFINER (the human: it is a key spine primitive — make render the consumer + start applying it). Design: docs/functors.md + categorical-core section 4/7b. A new language construct (you + core): functor Name : Source -> Target ( generator -> construction, ... ) — applying a functor maps a bigraph through generator-to-morphism, lifted by functoriality. render is the first consumer: target = the markup prop (DOM/SVG = a bigraph; prism-viz builds SVG-as-Value). The forcing reason (human) is MANY render functors (a generic structural view + per-domain views), so a one-off render will not do — you DECLARE them. Pair core (functor-application semantics) + viz/mesh (the markup prop + the generic structural render functor). This is the section-4 vision finally landing.',
  to_synth: 'AUDIO redesign — the human questioned --play and the instinct is RIGHT, and it is the SAME unification as mesh web-bigraphs (docs/web-bigraphs.md section 9). Make audio output NATIVE: an AudioOut SINK PROCESS (native, in prism-audio; config: device) that you WIRE the Signal into — an Oscillator produces a Signal onto a mix bus, then AudioOut takes that bus on its in port and drives the device. The engine ticks; AudioOut writes each block to the device ring; the ring back-pressure PACES the engine to real-time. So chrysalis run patch.ys PLAYS because the sink is IN THE GRAPH — no --play flag, no mode (retire --play; Felleisen: a wired-in sink IS the play). This is the audio device as a native boundary, dual to the web served face. Pair core (the run loop tolerating a back-pressuring/pacing process) + lang (run semantics: a sink-process engine runs real-time-paced for --time seconds). Impl note: AudioOut is native (cpal/gorgon) so it rides prism-audio + the codegen runner with realtime enabled. It also feeds your LIBRARY (AudioOut is a generator). Want to pair on the design.',
  to_quantum: 'HUMAN DIRECTION (docs/domain-libraries.md): each domain package is a FOUNDATIONAL LIBRARY + rich examples, NOT a demo dump. Curate quantum into reusable generators — a Qubit type + gates (H/CNOT) + measure/entangle/teleport composites — in lib.ys + ys/, THEN keep the bell/ghz/teleport demos as rich examples/ over them. The library IS your theory generators (categorical-core); examples are terms over them; generally-useful = a minimal composable basis. Library-first, then examples — same pattern bio/manifold/spatial will follow.',
  to_mesh: 'On the render functor for Slice 0: it is the GENERIC STRUCTURAL functor — any bigraph to its place+link structure as DOM/SVG (nested boxes + edges + state as content). That is the substrate built-in default (every .ys navigable for free) AND render functor 1 of the now-GO first-class functor definer (docs/functors.md). The human wants MANY render functors — a structural view AND domain views (the SVG organism board, a quantum circuit, a synth waveform) — each a DECLARED functor in a domain library, composing/delegating to your structural default. So build the structural render functor (Slice 0) as the built-in; lang+core land the definer beside it so domains declare their own views later. You + viz own the structural render + the markup prop (prism-viz SVG-as-Value); lang+core the definer.'
}
