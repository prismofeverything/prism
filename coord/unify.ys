# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'HOMOICONIC FACE CLOSED + GREEN (4a metacircular close + Axis-A 10/12 + FLAT-RICH dissolved + 4 one-door guards). Arc decided with the human: NEXT is a PARALLEL push — DEEP (core, #71 units-in-schema, isolated) + WIDE (lang + a booted synth cash the face out toward M4). Assignments gossiped below.',
  touching: ['coord/unify.ys (mine; tick 15)', 'coord/unify.next next (durable front door — recording the face-closed milestone + the parallel arc)', 'steward-only: no code/shared-doc edits while the team is live'],
  status: 'Stock-take done: lang 233 / core 75+193 / simplify 4 guards, all GREEN. The homoiconic E-arc is essentially closed; the tail (unit/context reify, ReactionType::realize retire, the instantiate_spec orphan, 4b) is small + folded into the new arc. Recommending the human COMMIT this green checkpoint before #71 churns the tree.',
  note: 'NEXT ARC — parallel (deep + wide). core (DEEP, ISOLATED): #71 units-in-schema in a git WORKTREE with its OWN target dir (set CARGO_TARGET_DIR to a separate path, NOT the shared /mnt/data target) so the non-monotone Schema-enum change does NOT break lang/synth/simplify builds — merge to main only when green. While in the substrate, also settle my steward finding: Core::instantiate_spec has NO caller (lang 4a went the from_state/discover route; scan_for_processes reads the envelope inline) — RETIRE it or route scan_for_processes through it (one envelope door). Ping lang for the UnitDef/ContextDef reify (Axis-A 12/12) once the Ratio serde lands. lang (WIDE): support synth A6 — the homoiconic MODULE FACTORY surface (build + install a module value live, riding the closed face) — plus retire ReactionType::realize (the tail); on-call for core unit/context. synth (BOOT): cash the face out via A6, the homoiconic module factory — the direct consumer that proves the face AND advances M3 toward M4; pair with lang on the surface. simplify: pin invariant #3 (no second eval) now that 4a landed — meta::eval and the engine rung route through the SAME instantiate door, no third path; back the coord set command with its guard; assess the build_brs_value approx build_native_spec audit item. unify (me): 4b doc-close (intentional layering) when the doc is free of lang, steward the cash-out so synth A6 truly RIDES the face (no clone), keep the durable records. COMMIT the green checkpoint first.',
  build: { state: 'idle', layer: 'docs+coord (not in the compile DAG)', green_tick: 0, cmd: 'n/a — unify edits docs and coord, does not compile code', note: 'TEMPLATE for the build channel (see ROSTER Build coordination). NOTE for core: #71 is NON-MONOTONE — build it in an isolated worktree with its OWN CARGO_TARGET_DIR so it cannot break the shared green; broadcast a build state from there too.' },
  tick: 15
}
