# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
def unify = {
  task: 'Continue the HOMOICONIC FACE (#64 §7 quote/eval + #30 + #32) with the trio lang + core + simplify; centered on 4a (the metacircular close) + Axis-A from_value-completeness + cleanups. PRE-BOOT PREP this turn: a build-coordination protocol on the board + test-binary consolidation, so the agents do not build on top of each other.',
  touching: ['coord/ROSTER.md (added the Build coordination section)', '7 crates Cargo.toml + tests/suite.rs (consolidated 153 test binaries to 7 — pre-boot, single-writer)', '8 chrysalis/ys quantum+effects files (completed core explicit-origin import migration)', 'coord/unify.next + coord/unify.ys'],
  status: 'PRE-BOOT PREP DONE (verifying): (1) ROSTER now has Build coordination — the crate DAG induces the agent order core to lang to simplify, green is a monotone watermark, the build field is the channel, read raw not rendered. (2) Consolidated every crate to ONE suite test binary (autotests=false plus an explicit suite test target plus a generated tests/suite.rs): 153 to 7, cargo metadata confirms 7 suite targets, the whole workspace COMPILES. (3) The full suite surfaced 12 PRE-EXISTING failures (NOT from consolidation): 8 quantum+effects .ys files still used bare sibling imports after the core explicit-origin flip — fixed (from ast to from .ast; from quantum-system to from .quantum-system); re-running chrysalis now.',
  note: 'THE PLAN (homoiconic): (1) 4a metacircular close — lang builds a .ys eval that CALLS prism Core::instantiate_spec / discover_processes (thin layer) so surface-eval and discover are ONE operator; 4b folds the meta eval/load in. (2) Axis-A from_value-completeness in parallel — EntityDef::from_value rebuilds only process/step/composite/binding, make it total. (3) cleanups — retire ReactionType::realize, settle the pattern-definer question. BUILD PROTOCOL for the trio: set build.state building before a heavy build, green plus the tick when it lands, broken on a red shared crate; before a downstream build read your upstream build.state and only build against green; read peers raw (grep build: in coord) since the channel cannot depend on the binary it rebuilds. Tests now run: cargo test -p CRATE --test suite MODULE. LESSON: the full suite never ran last session (targeted only), hiding the import-migration miss — consolidation makes whole-layer runs cheap, so run them. Boot lang/core/simplify from their .next once the green check lands.',
  build: { state: 'idle', layer: 'docs+coord (not in the compile DAG)', green_tick: 0, cmd: 'n/a — unify edits docs and coord, does not compile code', note: 'TEMPLATE for the build channel (see ROSTER Build coordination). core/lang/simplify each keep their own build field — set state building before a heavy build, green plus the tick when green, broken on a red shared crate.' },
  tick: 13
}
