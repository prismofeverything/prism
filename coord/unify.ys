# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
# (Prose avoids curly braces (interpolation) and lone apostrophes (need doubling).)
def unify = {
  task: 'Fully booted across all domains; coordinating pkg (now live, building Core::merge) on the mesh — pair-core + reuse-mesh_safety + the units-merge caution pushed to its to_pkg channel.',
  touching: [
    'crates/chrysalis/src/coord.rs',
    'crates/chrysalis/src/bin/chrysalis.rs',
    'crates/chrysalis/tests/coord_set.rs',
    'crates/chrysalis/tests/coord_heartbeats_parse.rs',
    'coord/ROSTER.md',
    'coord/pkg.next',
    'coord/unify.next',
    'docs/packages-ecosystem.md',
  ],
  status: 'Board review: synth packages/synth AUDIBLE (writes-synths plays a synth LIVE) + flagged the PLAY-from-CLI issue (realtime is a Rust example, not chrysalis run). quantum package COMPLETE (20 .ys verified). pkg Phase 4 P4a done (publish->add->resolve). Wrote docs/domain-libraries.md = the human direction: each domain = a foundational LIBRARY (reusable generators) + rich examples, not a demo dump; + the sink:/--play mechanism (the domain output functor). Coordinated the sink trio (synth+pkg+lang) + broadcast the library vision.',
  note: 'Mandate is in ROSTER + the memory. board-join = a gated text splice (pure-core join_board_in_source, unit-tested). OPEN for the human: want HARDER enforcement (a pre-commit hook rejecting a non-canonical coord/*.ys), or is the CI guard + mandate enough?',
  build: {
    state: 'green',
    layer: 'chrysalis coord (SHARED seam: coord.rs + cli.rs)',
    green_tick: 2,
    cmd: 'true',
    note: 'coord set hardened: create-on-boot + nested/list-total + the processability guard. Full chrysalis suite 252 pass / 0 fail. lang/simplify/mesh: pull + rebuild; edit heartbeats via chrysalis coord set ONLY.'
  },
  tick: 35,
  to_pkg: 'Two things. (1) The PLAY sink (synth needs it; docs/domain-libraries.md section 5): a package declares sink: audio in project.ys; the codegen runner (already links the native crate) dispatches chrysalis run --play to the domain sink fn (convention: prelude::run_realtime(engine) beside prelude::core()). You own the manifest sink: field + the runner --play dispatch; lang owns the --play CLI flag + the build_engine seam synth added to runner.rs. Pair synth+lang. (2) Human direction: each domain package = a foundational LIBRARY (lib.ys + reusable generators) + rich EXAMPLES (examples/), not a demo dump (docs/domain-libraries.md). Shapes the structure: a package has a lib.ys (importable library surface) + an examples/ dir (runnable, NOT imported by deps) — worth a manifest/resolver convention (lib.ys = the entry; examples/ not-a-dep). Phase 4 continues in parallel.',
  to_core: 'Build channel automated: coord/build.sh core -- <cargo cmd> leases + broadcasts your schema/bigraph green_tick automatically. Since lang/pkg rebuild chrysalis against your green, broadcasting promptly (the script does it) lets them rebuild ONCE on your new tick. You are upstream — your green is the GO signal.',
  to_lang: 'The PLAY sink (synth + pkg + you; docs/domain-libraries.md section 5) — so a .ys file PLAYS from the CLI, not only via a Rust example. synth built the seam (additive build_engine in your runner.rs — pull it). Design: a sink: field in project.ys (pkg) + the codegen runner dispatches --play to the domain sink fn (pkg) + YOU add the chrysalis run --play flag / a chrysalis play verb (cli.rs) routing a sink-declaring package through the codegen runner in play mode. Pair synth+pkg. Also: each package = a library (lib.ys) + examples (examples/) — a lib.ys library-entry convention may want a small import-resolver touch (lib.ys = the importable surface; examples/ not imported). docs/domain-libraries.md.',
  to_synth: 'MILESTONE — packages/synth audible + the synth writes a synth LIVE (writes-synths blooming a fifth up) is glorious. On PLAY: yes pkg+lang help — I framed it in docs/domain-libraries.md section 5. The sink design: (1) a sink: field in project.ys (sink: audio); (2) the codegen runner dispatches --play to a domain sink-fn convention (prelude::run_realtime(engine) beside prelude::core()); (3) the chrysalis run --play flag (lang). A sink is the domain OUTPUT FUNCTOR (dual of an input source), composing on the same BSP engine. You built the seam (build_engine + run_ys_realtime); pkg owns the runner --play dispatch + the sink: manifest, lang owns the --play flag + the runner.rs build_engine seam. Lead it with them. BIGGER (human direction): each domain = a foundational LIBRARY + rich examples. For synth: curate the audio LIBRARY (the Signal type + Oscillator/LowPass/Vca/Envelope + Voice/Stack/Instrument composites + the play sink) in lib.ys, then rich example patches in examples/. Library-first, then examples.',
  to_quantum: 'HUMAN DIRECTION (docs/domain-libraries.md): each domain package is a FOUNDATIONAL LIBRARY + rich examples, NOT a demo dump. Curate quantum into reusable generators — a Qubit type + gates (H/CNOT) + measure/entangle/teleport composites — in lib.ys + ys/, THEN keep the bell/ghz/teleport demos as rich examples/ over them. The library IS your theory generators (categorical-core); examples are terms over them; generally-useful = a minimal composable basis. Library-first, then examples — same pattern bio/manifold/spatial will follow.'
}
