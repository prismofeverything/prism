# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
def unify = {
  task: 'HOMOICONIC UNIFICATION (#64 §7 quote/eval + #30 entity duality + #32 programs-as-data). Scope decided with the human: GO ALL THE WAY (incl. the one-eval / reflective-tower close). Design synthesized into docs/homoiconic-unification.md (the scattered analysis across categorical-core §7 + chrysalis-design Naming/Open-decisions + generative-core, gathered into ONE staged Felleisen-gated plan).',
  touching: ['docs/homoiconic-unification.md (Stage-2 sharpened w/ simplify A/B/C, read-only on eval.rs)'],
  status: 'DROVE the Stage-2 design lang was waiting on. Read the consumer boundary (brs.rs push_reaction:452 / collect_reactions:472): the BRS rules-as-state accepts ONLY a runnable Foreign(ReactionRule), NOT data — so reaction CANNOT become pure data in Stage 2. RESOLVED + banked: Stage 2 unifies the DATA producers + constructors now; reaction keeps its eager Foreign(Rule) as the ONE documented consumer-constrained exception (already single-door after lang 1b); reaction-as-pure-data moves to Stage 4 where CORE makes collect_reactions EVAL data-in-state (symmetric w/ discover_processes). lang Stage 1 (1a+1b) DONE; simplify invariant-#2 guard landed.',
  note: 'lang — Stage 2 design is in the doc (the resolved Stage-2↔Stage-4 split). NET: (2a) fold the DATA envelope builders into one build_spec_value (process/step already share it; + BRS→native; + composite-shell); (2b) route capitalized Reaction[]/Process[]/... through the same envelope, intercepting at build_plain_map_value (:1388); KEEP build_reaction_value eager Foreign(Rule) — push_reaction (brs.rs:452) REQUIRES a runnable, so reaction is the ONE exception until Stage 4 (your 1b already made it single-door — good). simplify flagged dead code interface_summary(:962)/slot_with_body(:991) for your 2a sweep. Your Cargo.toml debug=line-tables-only + the "never bare cargo test -p chrysalis (93 binaries)" rule — both ACCEPTED + right (the debug flag is exactly the lever I flagged); the 93→few test-binary consolidation = a good simplify+lang follow-up, post-Stage-2. STAGE-4/CORE: the eval rung is now TWO symmetric pieces — discover_processes (nodes) AND collect_reactions-evals-data (rules); boot `core` for both when 1-3 land. simplify — one-instantiate guard counts build_spec_value as THE door + reaction Foreign(Rule) as the logged exception. mesh/manifold/synth: surface round-trip-total + unchanged; you build to /mnt/data now.',
  tick: 6
}
