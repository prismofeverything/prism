# coord/unify.ys — the `unify` peer's LIVE heartbeat (rebuilt from unify.next on boot).
# unify stewards the OVERALL (coord/unify.next is the project front door) and edits
# ONLY this file + coord/unify.next. coord/board.ys MERGES every peer's heartbeat
# through the mesh link — communication ACROSS THE LINK, no write races.
def unify = {
  task: 'HOMOICONIC UNIFICATION (#64 §7 quote/eval + #30 entity duality + #32 programs-as-data). Scope decided with the human: GO ALL THE WAY (incl. the one-eval / reflective-tower close). Design synthesized into docs/homoiconic-unification.md (the scattered analysis across categorical-core §7 + chrysalis-design Naming/Open-decisions + generative-core, gathered into ONE staged Felleisen-gated plan).',
  touching: ['docs/homoiconic-unification.md (Stage-2 sharpened w/ simplify A/B/C, read-only on eval.rs)'],
  status: 'STAGE 3 DONE (simplify — the one-door guards: invariant #2 round-trip totality + invariant #1 one-instantiate, all 3 reaction surfaces on one build_rule core). So Stages 1, 2a, 2b, 3 DONE + the 4c substrate. lang is TAKING THE 4c HANDOFF now (flip build_reaction_value→DATA) = the FLAT/RICH dissolution happening live — the face becomes uniform. The unified face is now PINNED against regression by the guards.',
  note: '4c HANDOFF coordination (lang) — after you flip build_reaction_value→DATA, RE-VERIFY green end-to-end, since the DATA now flows through core 4c (push_reaction-evals-data), not a Foreign carrier: (a) simplify''s guards (one_instantiate + roundtrip) pin build_rule = the LOWERING, so should hold — confirm; (b) alchemy + rules_as_state (reactions now ride the data path); (c) core''s reaction_data_as_state. And before RETIRING the ReactionType::realize re-wrap, confirm no non-BRS consumer needs it (core flagged this). AFTER 4c: 4a (lang+core — a .ys eval op CALLING discover_processes = the metacircular close) + the from_value-completeness prereq (gap 3). simplify — Stage 3 deliverable DONE; park or pick up the #46 consolidation tail (your call, the round-trip guard subsuming lang''s). core — 4c proven by lang''s flip; coordinate the re-verify, then pair on 4a + the recognition doc. mesh/manifold/synth: surface unchanged.',
  tick: 10
}
