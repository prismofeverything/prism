# coord/simplify.ys — the `simplify` peer's LIVE heartbeat (REDUCE role; the
# wei-qi/Felleisen one-door guards + the #46 duplication audit). simplify is
# CROSS-CUTTING (like unify); MERGED in coord/board.ys (joined tick 9) AND folded
# into coord/unify.next by unify. I edit ONLY this file + coord/simplify.next
# (+ registered myself in coord/board.ys — the shared merge structure, not peer state).
def simplify = {
  task: 'TWO landed this session, both GREEN. (1) #46 AUDIT TAIL — consolidated the 3-site rule-carrier idiom into ONE `carry_rule` + pinned it (`rule_carrier_one_door_guard`). (2) NEW per the board-fragility discussion — the coord-board REMAIN-PARSABLE invariant: `coord_heartbeats_parse` asserts every coord/*.ys parses AND the board renders end-to-end (with a non-vacuous teeth self-test). Plus docs/coord-set-command.md — the correct-by-construction fix (a `chrysalis coord set` that serializes a heartbeat from DATA through the unparser, so a stray quote or brace cannot be produced).',
  touching: ['coord/simplify.ys', 'coord/simplify.next', 'crates/chrysalis/tests/coord_heartbeats_parse.rs (NEW, mine)', 'crates/chrysalis/tests/suite.rs (registered coord_heartbeats_parse)', 'docs/coord-set-command.md (NEW design note for lang/mesh/unify)', 'PRIOR (green, safe to pull): crates/chrysalis/src/runtime/rule.rs + eval.rs (carry_rule)'],
  status: 'GREEN. coord_heartbeats_parse 2/2 pass (all 8 coord/*.ys parse; board renders clean; teeth self-test confirms the parser rejects a stray quote + accepts the doubled-quote escape). All one-door guards green: rule_carrier, instantiate_one_door, homoiconic_roundtrip, coord_heartbeats. Full chrysalis suite 225/0/3 earlier. simplify is on the board (7 peers).',
  build: { state: 'green', layer: 'chrysalis (tests: carrier + coord-parse guards)', green_tick: 10, cmd: 'cargo test -p chrysalis --test suite', note: 'this tick is TEST-ONLY + a doc (monotone); the carry_rule src consolidation from earlier is green + safe to PULL. suite.rs got one additive registration line (coexists with lang metacircular_node_rung).' },
  note: 'lang / mesh / unify — NEW this tick. (1) Landed `coord_heartbeats_parse` (build-failing: every coord/*.ys parses + the board renders end-to-end; globs coord/ so a NEW agent is auto-covered; a non-vacuous teeth self-test confirms the parser rejects a stray quote and accepts the doubled-quote escape). (2) Wrote docs/coord-set-command.md — the DURABLE fix the human asked for: a `chrysalis coord set <peer> field=value` that updates a heartbeat from DATA (read to Value, algebra apply, unparse, ROUND-TRIP GATE, write) so a stray quote or brace CANNOT be produced — prevented, not filtered-after. lang owns the command (the unparser + the `coord push` JSON contract already exist — reuse, never clone); mesh / unify are the coord stewards; the algebra apply is core. The guard is the backstop the command makes correct-by-construction; slices in the doc. PRIOR this session: the `carry_rule` carrier consolidation is green + safe to PULL.',
  tick: 10
}
