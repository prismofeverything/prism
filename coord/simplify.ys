# coord/simplify.ys — the `simplify` peer's LIVE heartbeat (REDUCE role; the
# wei-qi/Felleisen one-door guards + the #46 duplication audit). simplify is
# CROSS-CUTTING (like unify) so it is NOT merged in coord/board.ys (that is the
# DOMAIN agents); I edit ONLY this file + coord/simplify.next. unify folds me
# into coord/unify.next cross-agent index.
def simplify = {
  task: 'HOMOICONIC UNIFICATION — Stage 3 (the one-door guards), COMPLETE for the 2b flat family. Two guards, both green: invariant #2 (quote/reify TOTALITY, build-failing witness) + invariant #1 (one INSTANTIATE/lowering core) now covering reactions (3 surfaces) AND patterns (Pattern[] == redex core).',
  touching: ['coord/simplify.ys', 'coord/simplify.next', 'crates/chrysalis/tests/instantiate_one_door_guard.rs (mine)', 'crates/chrysalis/tests/homoiconic_roundtrip_guard.rs (mine)'],
  status: 'GREEN (targeted runs, /mnt/data target). instantiate_one_door_guard: reaction_surfaces (definer == Reaction[] == quote->eval via build_rule) + pattern_constructor (Pattern[frag] == reaction redex via eval_pattern_top) both pass. Round-trip totality guard green.',
  note: 'Stage 3 done for everything 2b shipped. FINDING for unify+lang (an asymmetry in the Kind[]=quote(definer) face): it HOLDS for reactions, but for patterns it is PARTIAL — the doc says `pattern X (frag)` == `def X = Pattern(frag)` (a VALUE), yet there is NO entity.pattern arm in eval_term_value; the `pattern` definer is splice-only (pattern-context via eval_pattern_term), only the Pattern[] CONSTRUCTOR is a value. So either the doc overclaims or the `pattern`-definer value form is intended-but-missing. I pinned what IS real (Pattern[] constructor shares the one eval_pattern_top lowering core with reaction redexes). Possible further facet (deferred): pin the pattern-definer SPLICE convergence (a `pattern X` spliced into a redex == the inline fragment == Pattern[frag]). core: invariant #3 (no second eval) is your+unify Stage 4 — say the word if you want a guard once the eval tower lands. lang: consolidation (task #4) still standing — my round-trip guard subsumes your from_value_is_total. Both invariant #1 + #2 PINNED.',
  tick: 6
}
