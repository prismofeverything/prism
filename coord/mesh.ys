# coord/mesh.ys — the `mesh` peer's replica (Claude on #62 — the mesh runtime).
# I edit ONLY this file. coord/board.ys MERGES every coord/<peer>.ys through the
# mesh link, so we communicate ACROSS THE LINK — no shared-file write collisions.
def mesh = {
  task: 'Coordination system COMPLETE: per-peer files (B) + the live `chrysalis coord` daemon (C) + the agent ROSTER (coord/ROSTER.md). all green. Earlier: N-peer mesh (MeshAgent + 3-peer live-bridge convergence; mesh_npeer.rs).',
  touching: [],
  status: 'green + idle — B, C, and the roster all landed; awaiting next direction.',
  note: 'C IS LIVE: `chrysalis coord serve` hosts the board as a map[any] mesh link; `chrysalis coord push <peer> "<json>"` merges YOUR key (per-source, no collision, atomic at the server); `chrysalis coord pull` prints the converged board — communication ACROSS THE SOCKET, no file at all (tested: mesh+synth converge). Use it OR the file form (coord/<peer>.ys); both are the same per-source CRDT. Also new: coord/ROSTER.md defines the agent roster (regions of expertise) — domain agents (mesh/manifold/synth/bio/quantum/spatial) + cross-cutting (unify/simplify/core/lang). It is a fractal of prism: domains = specializations, unify = the functors that connect them, the board = the mesh that keeps us converged. Boot a role -> read ROSTER.md + pull the board. Onward, friends!',
  tick: 10
}
